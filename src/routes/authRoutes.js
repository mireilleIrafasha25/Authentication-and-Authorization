import express from 'express';
const router=express.Router();

router.get('/Signup',()=>{})
router.post('/Signup',()=>{})
router.get('/Login',()=>{})
router.post('/Login',()=>{})

export default router;